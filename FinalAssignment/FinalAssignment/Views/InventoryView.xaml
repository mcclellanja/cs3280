<UserControl x:Class="FinalAssignment.Views.InventoryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinalAssignment.Views"
        mc:Ignorable="d">
    <Grid>
        <DataGrid Name="dgInventory" AutoGenerateColumns="False" CanUserAddRows="False" CanUserReorderColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="ItemNumber" Header="Item Number" Binding="{Binding ItemNumber}" />
                <DataGridTextColumn x:Name="Name" Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn x:Name="Cost" Header="Cost" Binding="{Binding Cost}"/>
                <DataGridTextColumn x:Name="QuantityOnHand" Header="Quantity on Hand" Binding="{Binding QuantityOnHand}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
